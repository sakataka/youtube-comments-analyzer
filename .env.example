YOUTUBE_API_KEY=
DATABASE_URL=
DATA_DIR=
# Explicit test data only; never enable for ordinary use.
YOUTUBE_FIXTURE_FALLBACK=
# Token-free local sentiment/emotion models (on by default). Set off to disable.
LOCAL_MODELS=
# X reactions via the logged-in Grok CLI (on by default, once per analysis). Set off to disable.
X_SEARCH=
# Optional: GROK_BIN, GROK_MODEL, X_SEARCH_TIMEOUT_SECONDS (default 480)
